import React, { Component } from 'react';

export default class CreateRoomPage extends Component {
    constructor (props) {
        super(props);
    }

    render() {
        return <h2>Create a Room NOW!</h2>;
    }
}